# Task: Repo bootstrap

Goal: Ensure the repo is buildable, testable, and releasable.

## Steps
- Add minimal CI for lint/test/build.
- Add container build (if service repo).
- Add smoke test instructions.
